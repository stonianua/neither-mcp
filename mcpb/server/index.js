#!/usr/bin/env node
/**
 * Claude Desktop MCPB entry point.
 *
 * Claude Desktop launches this file with its bundled Node runtime:
 *   node ${__dirname}/server/index.js
 * Cursor JSON config remains: npx -y @neitherai/mcp-server@latest
 */
import "@neitherai/mcp-server";
